[project]
name = "drop-to-markdown"
version = "0.1.0"
description = "Convert uploaded files to Markdown with MarkItDown."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "markitdown[all]>=0.1.0",
    "python-multipart>=0.0.9",
    "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
